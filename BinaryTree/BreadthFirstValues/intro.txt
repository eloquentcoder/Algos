Given the root of a binary tree, return the its node values by traversing across the breadth of the nodes

HINT: While for a depth first value we use a stack to traverse the tree, for a breadth first we use a queue